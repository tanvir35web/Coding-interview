var buildArray = function(nums) {
    ans = [];
    for(i = 0; i < nums.length; i++) {
        answer = nums[nums[i]]
        ans.push(answer)
        
    }
    return ans;
};

nums = [5,0,1,2,3,4]
console.log(buildArray(nums));